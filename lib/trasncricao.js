const vosk = require('vosk'); // ou outro serviço de transcrição, ex: Whisper
const fs = require('fs');

async function transcreverAudio(filePath) {
  // lógica de transcrição com biblioteca escolhida
  return 'Transcrição de exemplo'; // placeholder
}

module.exports = {
  transcreverAudio
};