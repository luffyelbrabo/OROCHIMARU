const emoji = require('./emoji.json');

exports.get = function(name) {
  return emoji[name] || `:${name}:`;
};

exports.search = function(term) {
  const results = [];
  for (let key in emoji) {
    if (key.includes(term)) results.push({ key, emoji: emoji[key] });
  }
  return results;
};